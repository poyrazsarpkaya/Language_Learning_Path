#include <stdio.h>

int main (){

    int level[] = {1,2,3,4,5,6,7};                        // C dilinde sayı türlerinde dizi oluşturma. 0 dan başlıyor
    double xp[] = {200,300.5,400,60};
    float score[] = {2.3,5,5,6,7};
    
    printf("\nlevel: %d xp: %lf score: %f", level[0], xp[1], score[1]);

    

    return 0;
}